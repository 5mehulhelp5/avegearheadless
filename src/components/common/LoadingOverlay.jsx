import React from 'react';

const LoadingOverlay = () => {
    return (
        <div className="loader-overlay">
            <div className="orange-pulse"></div>
        </div>
    );
};

export default LoadingOverlay;
